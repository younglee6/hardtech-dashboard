name: Update Hardtech Live Data

on:
  schedule:
    # 09:00 Asia/Shanghai = 01:00 UTC
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate live data JSON from whitelisted sites
        run: |
          python3 "Docs/demos/scripts/generate_live_data_from_whitelist.py" \
            --out "Docs/demos/site/hardtech-live-data.json" \
            --limit 10

      - name: Commit and push if changed
        run: |
          if git diff --quiet -- "Docs/demos/site/hardtech-live-data.json"; then
            echo "No changes in JSON."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "Docs/demos/site/hardtech-live-data.json"
          git commit -m "chore: update hardtech live data"
          git push
